# Kobold 灵动岛（Island）实现计划

> **状态（2026-09-20）**：全部阶段已实现并随 v1.0.1 发布；5.2（组件过多时的溢出）
> 于 2026-09-20 补齐（横向滚轮滚动），5.4 取消（实测无问题，不做自动隐藏）。
> 本文件保留为历史计划，复选框已按实际完成情况回填。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用屏幕顶部居中的"灵动岛"取代桌面文件夹图标——岛收起时是一个小药丸，hover 时展开显示各 widget 图标，点击图标呼出对应面板（面板收起即隐藏）；保留 lock（内容锁）与 pin（面板失焦不收起）。

**Architecture:** 新增 `IslandWindow`（顶部居中、Topmost、透明、无任务栏）作为唯一入口与状态机（collapsed 药丸 / expanded 图标行）。`FolderWidget` 从"桌面图标+面板"改造为"纯面板窗口"，由岛按其算好的位置显隐。`WidgetManager` 负责创建岛、把 widget 列表喂给岛、并协调面板显隐。

**Tech Stack:** WPF (net48)、C# 7.3、现有 `IconRendererFactory`（复用 widget 图标渲染）、`DispatcherTimer` + `DoubleAnimation`（收起/展开动画）。

## Global Constraints

- 目标框架 net48，`LangVersion 7.3`；所有 UI 文案走 `Localization`（en/zh/ja 三语，`tests/LangCheck` 守护 key 集合一致）。
- 显示缩放：进程已声明 PerMonitorV2（`app.manifest`）；涉及屏幕坐标一律用 WPF 单位（DIP），窗口尺寸/位置用 `SystemParameters`。
- 岛的参数（本计划定稿值）：收起态药丸 120×8（窗口含透明热区 300×10，便于 hover）；展开态高 56，每 widget 入口 44×44，间距 8；**鼠标离开 1.5s 后自动收起**（悬停期间不自动收起——避免光学鼠标微抖动重置静置定时器、也避免在光标下突然收起）；贴屏顶 y=0，水平居中。
- 面板位置：固定从岛下方展开（水平居中），不做自由拖动（简化）；面板宽度=现有 `CalculatePanelSize()`，高度同理。
- 保留：`lock`（禁止内容移入移出，菜单/拖拽仍在 `FolderWidget` 内）、`pin`（面板失焦/失焦后不收起）。
- 每个阶段结束后必须：`dotnet build Kobold.csproj -c Debug` 0 错误；改动含新词条时 `tests/LangCheck` 全绿。
- 临时验证脚本放 `%TEMP%\opencode\`，不进仓库。提交信息用 conventional commits（`feat(island): ...`）。

---

## 阶段 1：灵动岛外壳（可运行的岛）

**Files:**
- Create: `Controls/IslandWindow.xaml`、`Controls/IslandWindow.xaml.cs`
- Modify: `Core/WidgetConstants.cs`（岛尺寸常量）
- Modify: `App.xaml.cs`（启动时创建岛）

**Interfaces:**
- Produces: `IslandWindow` 类，含 `void SetWidgets(IEnumerable<FolderData>)` 与事件 `event Action<string> WidgetActivated`（folderId）。
- 常量：`ISLAND_PILL_WIDTH=120`、`ISLAND_PILL_HEIGHT=8`、`ISLAND_HOVER_WIDTH=300`、`ISLAND_HOVER_HEIGHT=10`、`ISLAND_EXPANDED_HEIGHT=56`、`ISLAND_TILE_SIZE=44`、`ISLAND_TILE_GAP=8`、`ISLAND_COLLAPSE_DELAY_MS=4000`、`ISLAND_LEAVE_DELAY_MS=1500`。

**行为：**
- Window：`WindowStyle=None`、`AllowsTransparency=True`、`Background=Transparent`、`Topmost=True`、`ShowInTaskbar=False`、`ResizeMode=NoResize`。
- 位置：`Left=(SystemParameters.PrimaryScreenWidth-Width)/2`、`Top=0`。
- collapsed：窗口 300×10，内部 120×8 圆角药丸居中（深色半透明 `#CC202020` + 圆角 4）。
- expanded：窗口宽=`24 + N*(44+8)`、高 56，圆角胶囊背景 `#E6202020`，内横排各 widget 图标入口（阶段 1 用占位图标，阶段 2 接真图标）。
- `MouseEnter` 于窗口 → 展开；`MouseLeave` → 启动 1.5s 收起定时器；展开后启动 4s 无操作收起定时器；任何交互重置。
- 动画：宽度/高度 `DoubleAnimation`（180ms）并同步重算 `Left` 保持居中。

**任务：**
- [x] 1.1 加岛尺寸常量到 `WidgetConstants.cs`。
- [x] 1.2 建 `IslandWindow.xaml(.cs)`，实现 collapsed/expanded 两种布局 + hover/定时器状态机（此阶段 expanded 内容为占位文字 "Kobold"）。
- [x] 1.3 `App.OnStartup` 中在 `WidgetManager.Instance.Initialize()` 后 `new IslandWindow().Show()`。
- [x] 1.4 构建 + 启动：确认顶部中间出现小药丸；hover 展开、移开 1.5s 收起、静置 4s 收起；位置居中且贴顶。
- [x] 1.5 提交 `feat(island): add top-center island shell with hover expand/collapse`。

**验证：** 手动观察（如上 1.4）。

---

## 阶段 2：岛显示各 widget 图标并可呼出面板

**Files:**
- Modify: `Controls/IslandWindow.xaml(.cs)`（渲染真图标 + 点击）
- Modify: `Core/WidgetManager.cs`（创建岛、喂数据、响应激活）
- Modify: `Controls/FolderWidget.xaml.cs`（新增 `ShowPanelHere(double left, double top)`）

**Interfaces:**
- `WidgetManager`：持有 `IslandWindow _island`；`RefreshIsland()` 用 `_config.Folders` 调 `_island.SetWidgets(...)`；订阅 `_island.WidgetActivated += id => ...`。
- `IslandWindow` 渲染每 widget：用 `IconRendererFactory.Create(config.IconStyle).Render(canvas, folder.Color)` 渲染 44×44 图标（复用现有渲染器），透明背景 tile，hover 高亮，ToolTip=widget 名。
- `FolderWidget.ShowPanelHere(left, top)`：设置窗口到指定位置并展开面板（阶段 2 先用现有 `TogglePanel` 的展开逻辑 + 定位）。

**任务：**
- [x] 2.1 `IslandWindow` 渲染 widget 图标网格（数据来自 `SetWidgets`），点击 tile 触发 `WidgetActivated`。
- [x] 2.2 `WidgetManager`：创建/持有岛；`Initialize` 后 `RefreshIsland()`；`WidgetActivated` → 找到 widget 并呼出面板（定位岛下方居中）；widget 增删后刷新岛。
- [x] 2.3 `FolderWidget.ShowPanelHere(left, top)`：定位 + 展开（若已展开则收起）。
- [x] 2.4 构建 + 启动：岛里出现 WORKING / WORKING-APPLICATION / My Folder 三个图标；点击任一 → 对应面板在岛下方展开。
- [x] 2.5 提交 `feat(island): render widget icons and open panels from the island`。

**验证：** 手动观察 + 现有 3 个 widget 均能呼出。

---

## 阶段 3：FolderWidget 改为纯面板（去掉桌面图标）+ 可拖动并记住位置

> 需求变更（用户确认）：面板不再是"固定从岛下方展开、不可拖动"。改为：面板可拖动（顶栏为把手），
> 每个 widget 记住自己被拖到的位置，收起后再展开仍回到该位置；从未拖过时默认岛下方居中。

**Files:**
- Modify: `Controls/FolderWidget.xaml`（隐藏 `FolderIconGrid`/`FolderNameText`；`ExpandedPanel` 归位左 0；顶栏加拖动手势）
- Modify: `Controls/FolderWidget.xaml.cs` / `Interactions.cs`（岛模式显隐 + 面板拖动 + 位置持久化）
- Modify: `Core/FolderData.cs`（新增 `PanelX`/`PanelY` 记住面板位置）
- Modify: `Core/WidgetManager.cs`（创建 widget 时不显示桌面形态；呼出/收起面板）

**行为：**
- widget 创建即处于"面板待命"：窗口隐藏，`FolderIconGrid`/`FolderNameText` `Collapsed`，`ExpandedPanel` `Canvas.Left=0`，窗口尺寸=面板尺寸。
- `ShowPanel(defaultLeft, defaultTop)` → 用 `PanelX/PanelY`（已记忆）或默认（岛下方居中）定位并 `Show()` + 展开。
- `HidePanel()` → 淡出后 `Hide()`。
- 顶栏拖动 → 移动面板窗口；松手持久化 `PanelX/PanelY`（`OnDataChanged`）。
- 越界（含多屏）自动收进屏幕工作区。
- `Window_Deactivated`：未 pin → `HidePanel`；pin → 保持。
- `Esc`：未 pin → `HidePanel`；pin → 只清选择。
- 保留 lock 内容锁与其菜单/拖拽约束不变；不再走 `TogglePanel` 的"图标↔面板"几何逻辑。

**任务：**
- [x] 3.1 构造函数隐藏图标元素、`ExpandedPanel` 归位为左 0；窗口尺寸=面板尺寸。
- [x] 3.2 新增 `ShowPanel(defaultLeft, defaultTop)` / `HidePanel()` / `IsPanelOpen`；`Deactivated`/`Esc` 改走 `HidePanel`（pin 分支保留）。
- [x] 3.3 顶栏拖动面板 → 松手写入 `FolderData.PanelX/PanelY` 并保存；`ShowPanel` 优先用记忆位置，越界收进屏幕。
- [x] 3.4 `WidgetManager`：widget 不再 `Show()` 桌面窗口；点击岛图标切换该面板显隐；`ShowAll`/`HideAll` 语义改为"展开所有面板 / 收起所有面板"。
- [x] 3.5 构建 + 启动：桌面**不再有**文件夹图标；点岛图标 → 面板出现；拖动顶栏 → 位置记住，收起再展开回原位；点空白 → 收起（未 pin）/保持（pin）；lock 仍生效。
- [x] 3.6 提交 `feat(island): make folder widgets draggable panel-only windows`。

**验证：** 手动观察上表；确认面板位置拖动后被记住（含重启）、lock 菜单/拖拽限制仍工作、pin 面板不因失焦收起。

---

## 阶段 4：托盘 / 新建 / 设置 / 迁移整合

**Files:**
- Modify: `Services/TrayIconService.cs`（Show/Hide 语义、新建 widget 后刷新岛）
- Modify: `Windows/SettingsWindow.xaml(.cs)`（可选：岛参数设置）
- Modify: `Core/Localization.cs`（新词条 ×3）

**行为：**
- 托盘 "Add New Widget" → 新建后在岛里出现，并呼出其空面板（或展开岛）。
- 托盘 "Show/Hide All Widgets" → 展开/收起全部面板（或改为"显示岛/隐藏岛"——二选一由实现时定，倾向"展开岛 + 全部面板收起"）。
- 设置页新增（可选，先做则加词条）：岛自动收起延迟、是否贴顶。
- 迁移：既有 `PosX/PosY` 不再用于定位（保留字段兼容）；已 pinned widget 首次呼出即展开保持。

**任务：**
- [x] 4.1 托盘菜单接岛（新建/显示/隐藏均刷新岛状态）。
- [x] 4.2 新词条（若做设置项）：`Settings_IslandCollapseDelay` 等，三语 + LangCheck。
- [x] 4.3 构建 + 全量检查（build/LangCheck/StorageOpsCheck）+ 启动冒烟。
- [x] 4.4 提交 `feat(island): wire tray, new-widget and settings to the island`。

---

## 阶段 5：打磨

**任务：**
- [x] 5.1 展开/收起动画细化（缓动、宽度与位置同步）。
- [x] 5.2 岛内 widget 过多时横向滚动/分页。
  - 2026-09-20 实现：tile 行放进 `ScrollViewer`（隐藏滚动条、无背景、裁到胶囊内），
    滚轮每格滚动一个 tile；**上限宽度 = 屏宽的 1/4**（用户确认：整屏封顶会让岛横在
    浏览器标签栏上，误点概率太高），装不下即滚动；宽度与滚动数学抽到 `Core/IslandLayout.cs`，
    由 `tests/IslandLayoutCheck` 守护（行宽/胶囊宽公式 / 1/4 上限 / 拟合边界 / 夹取 / 步进）。
- [x] 5.3 清理不再使用的桌面图标相关代码路径（确认无引用后删除，保持仓库节俭）。
- [x] 5.4 ~~全屏应用时手动可用性确认（暂不做自动隐藏）~~ → **已取消**（2026-09-20：用户实测未遇到相关问题，不做自动隐藏）。

---

## Self-Review

- **Spec 覆盖**：入口（岛）=阶段1；widget 图标与面板呼出=阶段2；去桌面图标/收起隐藏=阶段3；lock/pin 保留=阶段3；托盘/新建=阶段4；视觉打磨=阶段5。
- **风险**：Topmost 与管理员权限 IM 的 Z 序冲突（Windows 限制，托盘兜底）；岛展开态在多 widget 时宽度增长（阶段5 处理溢出）。
- **回滚**：分阶段提交，任一阶段不满意可停在上一提交。
